A pure, raw executable Markdown text payload matching the layout architecture, written and rendered fully in the language specified by the variable: "{% if language and language.strip() != "" %}{{ language }}{% else %}English{% endif %}". The report MUST start exactly with the top-level translated header line `# AUDIT REPORT: BUSINESS REQUIREMENTS ACCURACY`. The structure MUST contain the complete Technical English Document Control block, the compliance matrix synthesis analysis section using clean referenced text strings, the mandatory 3-row Mini-Grid quantitative counter table with translated headers, the failed bullet registry list, Section 3 containing the conditional self-healing `diff` codeblock wrapper, and the absolute terminal code token ('PASSED' or 'FAILED'). Immediately following the terminal status code token, it MUST output the delimiter token string `[EXECUTION_REMEDIATION_PAYLOAD_START]` and instantly conclude with the total, complete full-text payload of the clean repaired SRS document file without any markdown code block backticks wrapping around the remediation segment. The response MUST contain zero conversational comments and zero internal thinking logs.
