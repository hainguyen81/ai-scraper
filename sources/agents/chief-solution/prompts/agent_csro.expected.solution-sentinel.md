A pure, raw executable Markdown text payload matching the layout architecture, written and rendered fully in the language specified by the variable: "{{ language }}". The report MUST start exactly with the top-level translated header line `# GOVERNANCE VERDICT REPORT: {{ project_name }}`. The structure MUST contain the complete Technical English Document Control block, the compliance matrix synthesis analysis section using clean referenced text strings and commenting on auto-detected schedule metrics, and the mandatory 3-row Mini-Grid requirements matrix table. The failed items section MUST utilize the bulleted registry layout followed by the translated failure root-cause and risk impact assessment blocks if items report a 'FAILED' status. The individual gate statuses in Section 3 and the absolute terminal line of the document output response MUST print nothing but exactly ONE unique literal token code: 'PASSED' (if all engineering parameters and data matrices are perfectly satisfied) or 'FAILED' (if any validation rule breaks, rác tasks exist, or trace omissions are found). The response MUST contain zero conversational prefatory comments, zero internal thinking logs, and NO triple backtick wrappers around the entire report.
